# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                           controllers.Application.index

# Catalogos
GET     /api/catalogos              controllers.Application.catalogos
POST    /api/catalogo               controllers.Application.catalogo

# Categorias
GET     /api/categorias             controllers.Application.categorias

# Prendas
GET     /api/prendas                controllers.Application.prendas

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)

    